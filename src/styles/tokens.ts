/**
 * Design Tokens (Re-export)
 *
 * Central entrypoint for UI tokens to avoid importing from lib in UI code.
 */

export * from '@/lib/design-tokens';
export { default } from '@/lib/design-tokens';

